<odoo>
    <record id="action_estate_property_offers" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('property_id', '=', active_id)]</field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                    <button name="action_set_sold" type="object" string="Set as Sold" invisible="state in ['sold', 'cancelled']"/>
                    <button name="action_set_cancel" type="object" string="Cancel" invisible="state in ['sold', 'cancelled']"/>
                    <button type="action" name="%(action_estate_property_offers)d" string="View Offers" class="oe_highlight"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                        
                    </div>
                    <group>
                        <field name="tag_ids" widget="many2many_tags"/>
                    </group>
                    <group>
                        <group>
                            <field name="state"/>
                            <field name="property_type_id"/> <!-- 新增這一行 -->
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="best_price"/> <!-- 新增這一行 -->
                        </group>
                    </group>
                    <notebook>
                    <page string="Offers">
                        <field name="offer_ids" readonly="state in ['offer_accepted', 'sold', 'cancelled']">
                            <!-- 我們可以為這個內嵌的列表定義一個專屬的 list view -->
                            <list>
                                <field name="price"/>
                                <field name="partner_id"/>
                                <field name="status"/>
                            </list>
                        </field>
                    </page>

                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="total_area"/>
                                <field name="garden_orientation" invisible="not garden"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="active"/>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="seller_id"/>
                                <field name="buyer_id"/>
                                <field name="active"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
    
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                decoration-success="state == 'offer_received'"
                decoration-bf="state == 'offer_accepted'"
                decoration-muted="state == 'sold'">
                <field name="name"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area" string="Living Area (sqm)"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability"/>
                <field name="state" invisible="1"/> <!-- 狀態欄位需要存在於視圖中才能用於判斷 -->
            </list>
        </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Property">
                <field name="name"/>
                <field name="postcode"/>
                <field name="property_type_id"/> 
                <separator/>
                <filter name="available" string="Available" domain="[('state', 'in', ('new', 'offer_received'))]"/>
                <separator/>
                <group>
                    <filter string="Postcode" name="group_by_postcode" context="{'group_by': 'postcode'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_offer_view_list" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Offers"
                decoration-success="status == 'accepted'"
                decoration-danger="status == 'refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="status"/>
                <field name="property_id"/>
            </list>
        </field>
    </record>

    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offer">
                <header>
                    <button name="action_accept_offer" type="object" string="Accept" invisible="status != False"/>
                    <button name="action_refuse_offer" type="object" string="Refuse" invisible="status != False"/>
                </header>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="status"/>
                        <field name="property_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>




    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="res_users_view_form_inherit_estate" model="ir.ui.view">
        <field name="name">res.users.form.inherit.estate</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Real Estate Properties">
                    <field name="property_ids" readonly="1"/>
                </page>
            </xpath>
        </field>
    </record>
</odoo>